[package]
name = "osm-cli"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "osm"
path = "src/main.rs"

[dependencies]
osm-core = { path = "../core" }
clap = { version = "4", features = ["derive"] }

[dev-dependencies]
assert_cmd = "2"
predicates = "3"
